generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model IFile {
  id          String  @id @default(cuid())
  description String?
  name        String
  url         String
  size        Int?
}
